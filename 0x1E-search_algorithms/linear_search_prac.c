#include "search_algos.h"
/**
 * linear_search - searches for particular value in an array
 * @array : array of integers
 * @size : size of array
 * @value : value to be looked for
 * returns index of value or -1 if value isn not found.
*/
int linear_search(int *array, int size, int value){
    int i;
    if (array == NULL)
    {
        return (-1);
        for (i = 0; i< size; i++){
            printf(*array)
            if *array == value
            return(i)
            array++
    else{
        printf(array[i] is [i])
    }
    return(-1)
        }