#include "main.h"

/**
 * mul - multiplies two integers
 * @a: Stores first argument
 * @b: Stores second argument
 * Return: multiple if properly executes
*/

int mul(int a, int b)
{
int mul = 0;
mul = a * b;
return (mul);
}
