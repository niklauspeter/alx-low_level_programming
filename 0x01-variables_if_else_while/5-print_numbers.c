#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - prints base 10 numbers from 0
 * Return:0 if exits properly
*/

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n")
	return (0);
}
